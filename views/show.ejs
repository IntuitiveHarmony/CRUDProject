<!DOCTYPE html>
<html lang="en">
<head>
    <%-include('./partials/head.ejs')%>
</head>
<body>
  <header>
    <%-include('./partials/nav.ejs')%>
  </header>
<div class = 'imgBack'>
  <h1><%=item.name%></h1>
  <img src="<%=item.img%>" alt="<%= item.name %>" class ='imgShow'>
  <h3>Style: <%=item.style%></h3>
  <p>Size: <%=item.size%></p>
  <h3>Price: $<%=item.price%></h3>
  <% for (let i=0; i < item.tags.length; i++ ) { %>
    <p>Tags: <a href = ''><%=item.tags[i]%></a><p>
  <%  } %>

  <a href='/edit/<%=item._id%>'>edit this item</a>

  <form action="<%=item.id%>?_method=DELETE" method="post">
    <input type='submit' value ='DELETE ITEM'>
  </form>
</div>



</body>
</html>
